import React from 'react';
import ReactDOM from 'react-dom';

const title = 'ㅅ니기니ㅏㅎ??';

ReactDOM.render(
  <div>{title}{title}</div>,
  document.getElementById('root')
);

module.hot.accept();